// for Excel import - currently inactive

export default interface clientSchema {
    name: string,
    taxnumber: string,
    segment: string,
    headquarters: string,
    foundation: string,
    _id?: string
}